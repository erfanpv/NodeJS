const fs = require("fs");

// const readableStream = fs.createReadStream("./examples/write.txt");
// readableStream.on("data", (chuk) => {
//   console.log(chuk.length);
// });

// const writebleStram = fs.createWriteStream("./examples/stream.txt");

// readableStream.pipe(writebleStram);

// const writebleStram = fs.createWriteStream("./examples/new.txt");
// writebleStram.write("Heloo good mOrinig");
// writebleStram.end("ok");
